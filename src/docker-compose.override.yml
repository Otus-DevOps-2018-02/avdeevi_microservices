version: '3.3'

services:
  ui:
    command: puma  --debug -w 2
    volumes:
      - /home/docker-user/ui_src:/app


  post:
    volumes:
      - /home/docker-user/post_src:/app

  comment:
    volumes:
      - /home/docker-user/comment_src:/app




